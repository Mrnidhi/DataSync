# Rename to 20-secret.yaml and fill in real values before applying.
# Better: create the secret out-of-band with `kubectl create secret generic ...`.
apiVersion: v1
kind: Secret
metadata:
  name: datasync-secrets
  namespace: datasync
type: Opaque
stringData:
  OPENAI_API_KEY: "sk-replace-me"
  GIT_TOKEN: "ghp-replace-me"
  SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/replace/me"
